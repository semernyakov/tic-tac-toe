# 🎮 Tic-Tac-Toe Frontend

React + TypeScript интерфейс для игры крестики-нолики с интеграцией Telegram ботов для получения промокодов.

## ✨ Особенности

- 🎯 **Умный AI противник** с алгоритмом Minimax
- 💜 **Современный дизайн** с градиентами и анимациями
- 📱 **Telegram интеграция** - кликабельная ссылка на бота
- 🔄 **Подсказки для игрока** - выделение выигрышных комбинаций
- 📱 **Адаптивный дизайн** - работает на всех устройствах
- 🎨 **Tailwind CSS** - утилитарный CSS фреймворк
- ⚡ **Vite** - быстрая сборка и разработка

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Настройка окружения

1. Создайте файл `.env`:
```env
VITE_API_URL=http://localhost:8000
```

2. Для production добавьте production файл `.env.production`:
```env
VITE_API_URL=https://ваш-backend-url.com
```

### Запуск разработки

```bash
# Запуск dev сервера
npm run dev

# Откроется на http://localhost:5173
```

### Сборка для продакшена

```bash
# Сборка
npm run build

# Предварительный просмотр собранного билда
npm run preview
```

## 🎮 Игровая механика

### Правила игры

- Игрок играет крестиками (X)
- Компьютер играет ноликами (O)
- Цель: собрать три символа в ряд (горизонтально, вертикально или по диагонали)

### Особенности AI

- **Алгоритм Minimax** с альфа-бета отсечением
- **Адаптивная сложность** - не использует подсказки при расчете ходов
- **Стратегическое мышление** - блокирует выигрыши игрока
- **Рандомизация** - выбирает между лучшими ходами случайно

### Подсказки

- Кнопка для включения/выключения подсказок
- Выделяет заранее выбранную выигрышную комбинацию
- AI избегает занятия выделенных клеток при включенных подсказках

## 🎨 Дизайн и стили

### Цветовая схема

```css
/* Основные цвета */
--pink-400: #f472b6
--pink-500: #ec4899
--purple-400: #c084fc
--purple-500: #a855f7
--purple-600: #8b5cf6
--purple-700: #7c3aed

/* Градиенты */
bg-gradient-to-r from-pink-500 to-purple-600
bg-gradient-to-br from-pink-50 via-purple-50 to-blue-50
```

### Компоненты

- **Игровое поле**: 3x3 сетка с hover эффектами
- **Модальные окна**: Результаты игры с анимациями
- **Кнопки**: Градиенты с hover и active состояниями
- **Иконки**: Lucide React иконки

## 🔧 Техническая информация

### Структура проекта

```
frontend/
├── src/
│   ├── App.tsx          # Основной компонент игры
│   ├── index.tsx        # Точка входа
│   └── index.css        # Глобальные стили
├── public/              # Статические файлы
├── package.json         # Зависимости
├── vite.config.ts       # Конфигурация Vite
├── tsconfig.json        # Конфигурация TypeScript
├── tailwind.config.cjs  # Конфигурация Tailwind
├── .env                 # Локальные переменные
├── .env.production      # Production переменные
└── README.md           # Документация
```

### Зависимости

```json
{
  "dependencies": {
    "lucide-react": "^0.263.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.15",
    "@types/react-dom": "^18.2.7",
    "@vitejs/plugin-react": "^4.0.3",
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.24",
    "tailwindcss": "^3.3.0",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
```

### Конфигурация Vite

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    host: true
  }
})
```

## 📡 Интеграция с Backend

### API взаимодействие

**Отправка результата игры:**
```typescript
const sendGameResult = async (result: string, promoCode?: string) => {
  const response = await fetch(`${API_URL}/api/game-result`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ result, promo_code: promoCode }),
  });
  return response.json();
};
```

**Получение промокода:**
```typescript
const generatePromoCode = () => {
  return Math.floor(10000 + Math.random() * 90000).toString();
};
```

### Telegram бот

**Кликабельная ссылка:**
```jsx
<p className="text-purple-500 text-xs">
  Код отправлен в телеграм-бот{' '}
  <a 
    href="https://t.me/TicTocToeBot" 
    target="_blank" 
    rel="noopener noreferrer"
    className="text-purple-600 hover:text-purple-800 underline"
  >
    @TicTocToeBot
  </a>
  :
</p>
```

## 🔧 Разработка

### Команды

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Предварительный просмотр
npm run preview

# Проверка типов
npm run type-check

# Линтинг
npm run lint
```

### Настройка IDE

**VS Code рекомендуемые расширения:**
- ES7+ React/Redux/React-Native snippets
- Tailwind CSS IntelliSense
- TypeScript Importer
- Auto Rename Tag
- Prettier - Code formatter

### Отладка

1. **Откройте Developer Tools** (F12)
2. **Проверьте Network вкладку** для API запросов
3. **Используйте Console** для логов
4. **Проверьте React DevTools** для состояния компонентов

## 📱 Адаптивность

### Брейкпоинты Tailwind

```css
/* Mobile First подход */
sm: 640px   /* Планшеты */
md: 768px   /* Малые ноутбуки */
lg: 1024px  /* Ноутбуки */
xl: 1280px  /* Большие экраны */
```

### Адаптивные классы

```jsx
<div className="max-w-md w-full">
  {/* Контейнер с максимальной шириной */}
  
  <div className="grid grid-cols-3 gap-3">
    {/* Адаптивная сетка */}
  </div>
  
  <button className="flex-1 px-4 py-3 sm:px-6 sm:py-4">
    {/* Адаптивные отступы */}
  </button>
</div>
```

## 🚀 Деплой

### Vercel (Бесплатно, рекомендуется)

**Пошаговая инструкция:**

1. **Создайте аккаунт** на [vercel.com](https://vercel.com)
2. **Импортируйте ваш GitHub репозиторий**
3. **Vercel автоматически определит Vite** и React приложение
4. **Настройте переменные окружения:**
   - Перейдите в Settings → Environment Variables
   - Добавьте: `VITE_API_URL=https://tic-tac-toe-production-2050.up.railway.app`
5. **Деплой произойдет автоматически**

**Пример рабочего frontend:** [https://tic-tac-toe-lime-beta.vercel.app/](https://tic-tac-toe-lime-beta.vercel.app/)

**Проверка деплоя:**
- Откройте URL вашего Vercel проекта
- Игра должна загрузиться без ошибок
- Проверьте Network tab в DevTools - API запросы должны идти на правильный backend

### Netlify (Альтернатива)

1. **Создайте новый сайт** на [netlify.com](https://netlify.com)
2. **Подключите репозиторий** с frontend кодом
3. **Настройте конфигурацию:**
   - **Build command:** `npm run build`
   - **Publish directory:** `dist`
   - **Node version:** 18 или выше
4. **Добавьте переменные окружения:**
   - `VITE_API_URL=https://ваш-railway-url.up.railway.app`
5. **Деплой** начнется автоматически

### Render.com (Полная альтернатива)

Для тех, кто предпочитает один сервис для всего:

1. **Создайте Static Site** на [render.com](https://render.com)
2. **Подключите репозиторий** (только папка frontend)
3. **Настройте конфигурацию:**
   - **Build Command:** `cd frontend && npm install && npm run build`
   - **Publish Directory:** `frontend/dist`
4. **Добавьте переменные окружения:**
   - `VITE_API_URL=https://ваш-backend-url.com`

### Ручной деплой

```bash
# 1. Сборка приложения
npm run build

# 2. Результат сборки находится в папке dist/
# 3. Загрузите содержимое папки dist/ на ваш веб-сервер

# Структура dist/:
# ├── index.html
# ├── assets/
# │   ├── index-[hash].js
# │   └── index-[hash].css
# └── ...
```

### Переменные окружения в production

**Vercel/Netlify Dashboard:**
```
VITE_API_URL=https://tic-tac-toe-production-2050.up.railway.app
```

**Локальная сборка для production:**
```bash
# Создайте .env.production файл
echo "VITE_API_URL=https://tic-tac-toe-production-2050.up.railway.app" > .env.production

# Соберите с production переменными
npm run build
```

### Автоматический деплой

При каждом push в главную ветку GitHub:

- 🚄 **Vercel** автоматически пересоберет и задеплоит frontend
- 🌐 **Netlify** автоматически пересоберет и задеплоит frontend
- ⚡ **Время деплоя:** ~2-3 минуты

### Docker для production

```dockerfile
# Multi-stage build для оптимизации размера
FROM node:18-alpine as builder

WORKDIR /app

# Копируем package файлы
COPY package*.json ./
RUN npm ci --only=production

# Копируем исходный код
COPY . .

# Сборка приложения
RUN npm run build

# Production stage
FROM nginx:alpine

# Копируем собранное приложение
COPY --from=builder /app/dist /usr/share/nginx/html

# Копируем nginx конфигурацию
COPY nginx.conf /etc/nginx/nginx.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

**nginx.conf:**
```nginx
events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    server {
        listen 80;
        server_name localhost;
        root /usr/share/nginx/html;
        index index.html;

        # SPA routing
        location / {
            try_files $uri $uri/ /index.html;
        }

        # Cache static assets
        location /assets/ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
}
```

## 🐛 Решение проблем

### Проблемы сборки

```bash
# Очистите кэш
rm -rf node_modules package-lock.json
npm install

# Проверьте версии Node.js
node --version  # Требуется v16+
```

### Проблемы с API

1. **Проверьте CORS настройки** в backend
2. **Убедитесь что backend запущен** на указанном порту
3. **Проверьте переменную VITE_API_URL** в .env файле
4. **Используйте Network tab** в DevTools для диагностики

### Проблемы с TypeScript

```bash
# Проверка типов
npx tsc --noEmit

# Обновление типов
npm install @types/react @types/react-dom
```

### Проблемы со стилями

1. **Проверьте Tailwind конфигурацию**
2. **Убедитесь что PostCSS настроен**
3. **Проверьте импорт CSS файлов**
4. **Используйте DevTools для проверки стилей**

## 📊 Производительность

### Оптимизации

- **Code splitting** с Vite
- **Tree shaking** для неиспользуемого кода
- **Минификация CSS и JS** в production
- **Lazy loading** для иконок

### Метрики

- **First Contentful Paint** < 1.5s
- **Largest Contentful Paint** < 2.5s
- **Cumulative Layout Shift** < 0.1
- **First Input Delay** < 100ms

## 🎨 Кастомизация

### Изменение темы

```css
/* tailwind.config.cjs */
module.exports = {
  theme: {
    extend: {
      colors: {
        'custom-pink': '#your-color',
        'custom-purple': '#your-color'
      }
    }
  }
}
```

### Добавление новых компонентов

```typescript
// src/components/NewComponent.tsx
import React from 'react';

interface Props {
  title: string;
}

export const NewComponent: React.FC<Props> = ({ title }) => {
  return (
    <div className="p-4 bg-purple-100 rounded-lg">
      <h2 className="text-purple-800">{title}</h2>
    </div>
  );
};
```

## 📞 Поддержка

- 📖 Проверьте [корневой README](../README.md) для общей документации
- 🔍 Изучите код в `src/App.tsx` для понимания логики
- 🧪 Тестируйте в браузере после изменений
- 📡 Проверяйте взаимодействие с backend API

---

**Frontend создан для обеспечения отличного пользовательского опыта игры.**
