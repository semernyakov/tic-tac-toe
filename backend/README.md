# 🎮 Tic-Tac-Toe Backend

FastAPI сервер для игры крестики-нолики с интеграцией Telegram для отправки промокодов.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Настройка окружения

1. Скопируйте пример конфигурации:
```bash
cp .env.example .env
```

2. Отредактируйте `.env` файл:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

### Запуск сервера

```bash
# Режим разработки
uvicorn main:app --reload --port 8000

# Или с автоматической перезагрузкой
python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

## 📡 API Endpoints

### Основные эндпоинты

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Информация о сервере |
| `/api/health` | GET | Проверка состояния системы |
| `/api/debug` | GET | Отладочная информация |
| `/api/game-result` | POST | Обработка результата игры |

### Примеры использования

**Проверка здоровья:**
```bash
curl http://localhost:8000/api/health
```

**Отправка результата игры:**
```bash
curl -X POST http://localhost:8000/api/game-result \
  -H "Content-Type: application/json" \
  -d '{"result": "win", "promo_code": "12345"}'
```

## 🤖 Telegram интеграция

### Настройка бота

1. **Создание бота:**
   - Найдите [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте `/newbot` и следуйте инструкциям
   - Сохраните полученный токен

2. **Получение Chat ID:**
   - Отправьте сообщение вашему боту
   - Откройте: `https://api.telegram.org/bot<ТОКЕН>/getUpdates`
   - Найдите `"chat":{"id":123456789}`

### Конфигурация

```env
# Токен вашего Telegram бота
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Chat ID для получения уведомлений
TELEGRAM_CHAT_ID=123456789
```

### Тестирование отправки

```bash
# Тест победы с промокодом
curl -X POST http://localhost:8000/api/game-result \
  -H "Content-Type: application/json" \
  -d '{"result": "win"}'

# Тест поражения
curl -X POST http://localhost:8000/api/game-result \
  -H "Content-Type: application/json" \
  -d '{"result": "lose"}'

# Тест ничьи
curl -X POST http://localhost:8000/api/game-result \
  -H "Content-Type: application/json" \
  -d '{"result": "draw"}'
```

## 📊 Ответы API

### Успешный ответ (победа)
```json
{
  "status": "success",
  "promo_code": "12345",
  "telegram_sent": true
}
```

### Успешный ответ (поражение/ничья)
```json
{
  "status": "success",
  "telegram_sent": true
}
```

### Отладочная информация
```json
{
  "telegram_bot_token_set": true,
  "telegram_chat_id": 123456789,
  "telegram_chat_id_type": "int"
}
```

## 🔧 Техническая информация

### Зависимости

- **FastAPI** - веб-фреймворк
- **httpx** - HTTP клиент для Telegram API
- **pydantic** - валидация данных
- **uvicorn** - ASGI сервер
- **python-dotenv** - управление переменными окружения

### Структура проекта

```
backend/
├── main.py              # Основное приложение FastAPI
├── config.py            # Конфигурация (не используется)
├── requirements.txt     # Зависимости
├── .env                 # Переменные окружения (секреты)
├── .env.example         # Пример конфигурации
├── .gitignore           # Игнорируемые файлы
└── README.md           # Документация
```

### Архитектура

- **FastAPI приложение** с CORS middleware
- **Асинхронная обработка** запросов
- **Интеграция с Telegram Bot API**
- **Генерация промокодов** (5-значные числа)
- **Подробное логирование** операций

## 🛡️ Безопасность

### Переменные окружения

- ❗ **НЕ коммитьте .env файл** в git
- 🔐 **Используйте .gitignore** для исключения секретов
- 🔄 **Регулярно обновляйте токены** бота
- 🚫 **Не передавайте токены** в публичных местах

### Production рекомендации

1. **Используйте секретное хранилище:**
   - AWS Secrets Manager
   - HashiCorp Vault
   - Системные переменные окружения

2. **Настройте HTTPS:**
   - Используйте обратный прокси (nginx)
   - Получите SSL сертификат

3. **Мониторинг:**
   - Настройте логирование
   - Мониторьте API вызовы
   - Отслеживайте ошибки Telegram

## 🐛 Решение проблем

### Сервер не запускается

```bash
# Проверьте зависимости
pip list | grep fastapi

# Проверьте порт
lsof -i :8000

# Проверьте Python версию
python --version
```

### Telegram не работает

1. **Проверьте токен:**
   ```bash
   curl https://api.telegram.org/bot<ТОКЕН>/getMe
   ```

2. **Проверьте Chat ID:**
   - Отправьте сообщение боту
   - Откройте: `https://api.telegram.org/bot<ТОКЕН>/getUpdates`

3. **Проверьте переменные:**
   ```bash
   curl http://localhost:8000/api/debug
   ```

### CORS ошибки

CORS уже настроен для всех источников. Если проблемы:

1. Проверьте, что frontend отправляет правильные заголовки
2. Убедитесь, что используется правильный API URL
3. Проверьте браузерную консоль на ошибки

## 📈 Производительность

### Оптимизация

- **Асинхронная обработка** всех запросов
- **Таймауты** для внешних API (30 сек)
- **Правильная обработка ошибок** без крахов
- **Минимальные зависимости**

### Масштабирование

- **Gunicorn + Uvicorn workers** для production
- **Docker контейнеры** для изоляции
- **Load balancer** для распределения нагрузки
- **Database** для хранения статистики игр

## 📋 Логирование

Сервер выводит подробные логи:

```
🚀 ЗАПУСК СЕРВЕРА
Бот токен: ✅ Есть
Chat ID: ✅ 123456789

🎮 Получен результат игры: win
   Промокод: 12345

📤 Отправка в Telegram:
   Chat ID: 123456789
   Сообщение: 🎉 <b>Победа!</b>...
   ✅ Успешно отправлено! Message ID: 1
```

## 🚀 Деплой

### Railway.app (Бесплатно, рекомендуется)

**Пошаговая инструкция:**

1. **Создайте аккаунт** на [railway.app](https://railway.app)
2. **Нажмите "New Project"** → "Deploy from GitHub"
3. **Подключите репозиторий** с вашим проектом
4. **Выберите папку backend** (Railway предложит выбрать)
5. **Добавьте переменные окружения:**
   ```bash
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   TELEGRAM_CHAT_ID=ваш_chat_id
   ```
6. **Railway автоматически определит FastAPI** и запустит приложение

**Пример рабочего backend:** [tic-tac-toe-production-2050.up.railway.app](https://tic-tac-toe-production-2050.up.railway.app)

**Проверка деплоя:**
```bash
curl https://ваш-проект.up.railway.app/api/health
# Должен вернуть: {"status":"healthy"}
```

### Render.com (Альтернатива)

1. **Создайте Web Service** на [render.com](https://render.com)
2. **Подключите репозиторий** с backend кодом
3. **Настройте конфигурацию:**
   - **Environment:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `uvicorn main:app --host 0.0.0.0 --port $PORT`
4. **Добавьте переменные окружения** в панели Render
5. **Деплой** произойдет автоматически

### Docker

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

## 📞 Поддержка

- 📖 Проверьте [корневой README](../README.md) для полной документации
- 🔍 Изучите логи сервера для диагностики
- 🧪 Используйте `/api/debug` для проверки конфигурации
- 📡 Тестируйте API через curl команды

---

**Backend создан для обеспечения надежной и масштабируемой работы игры.**
