# 🎮 Крестики-нолики с Telegram интеграцией

Полнофункциональная игра в крестики-нолики с интеграцией Telegram для получения промокодов при победе.

## ✨ Особенности

- 🎯 **Умный AI противник** с алгоритмом Minimax и альфа-бета отсечением
- 💜 **Современный дизайн** с градиентами и анимациями
- 📱 **Telegram интеграция** - получение промокодов через бота [@TicTocToeBot](https://t.me/TicTocToeBot)
- 🔄 **Подсказки для игрока** - выделение выигрышных комбинаций
- 🏆 **Генерация промокодов** - автоматические 5-значные коды
- 🚀 **Production-ready** - готов к развертыванию

## 🏗️ Архитектура

```
├── backend/          # FastAPI сервер
│   ├── main.py       # Основное приложение
│   ├── requirements.txt
│   └── .env          # Конфигурация Telegram
├── frontend/         # React + TypeScript
│   ├── src/
│   │   ├── App.tsx   # Игровая логика
│   │   ├── index.tsx
│   │   └── index.css # Стили
│   ├── package.json
│   └── .env          # API URL
└── README.md
```

## 🚀 Быстрый запуск

### 1. Backend

```bash
cd backend

# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env файл

# Запуск сервера
uvicorn main:app --reload --port 8000
```

### 2. Frontend

```bash
cd frontend

# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

Откройте [http://localhost:5173](http://localhost:5173) в браузере.

## 🤖 Настройка Telegram бота

### Создание бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Выберите имя и username для бота
4. Сохраните полученный токен

### Получение Chat ID

1. Отправьте любое сообщение вашему боту
2. Откройте в браузере:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
3. Найдите `"chat":{"id":123456789}` - это ваш Chat ID

### Конфигурация

**Backend (.env):**
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

**Frontend (.env):**
```env
VITE_API_URL=http://localhost:8000
```

## 🎮 Игровая механика

### Правила игры
- Вы играете крестиками (X)
- Компьютер играет ноликами (O)
- Цель: собрать три символа в ряд

### Особенности AI
- **Алгоритм Minimax** с альфа-бета отсечением
- **Адаптивная сложность** - не использует подсказки при расчете ходов
- **Стратегическое мышление** - блокирует выигрыши игрока

### Подсказки
- Включите подсказки для выделения выигрышных комбинаций
- AI не будет занимать выделенные клетки при подсказках
- Подсказки можно включить/выключить кнопкой

## 📡 API Endpoints

### Backend API

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Информация о сервере |
| `/api/health` | GET | Проверка состояния |
| `/api/debug` | GET | Отладочная информация |
| `/api/game-result` | POST | Отправка результата игры |

### Примеры запросов

**Проверка здоровья:**
```bash
curl http://localhost:8000/api/health
# Ответ: {"status":"healthy"}
```

**Отправка результата игры:**
```bash
curl -X POST http://localhost:8000/api/game-result \
  -H "Content-Type: application/json" \
  -d '{"result": "win", "promo_code": "12345"}'
```

**Ответ:**
```json
{
  "status": "success",
  "promo_code": "12345",
  "telegram_sent": true
}
```

## 🎨 Дизайн

### Цветовая схема
- **Основные цвета**: Розовый (#ec4899) и Фиолетовый (#8b5cf6)
- **Градиенты**: От розового к фиолетовому
- **Фон**: Мягкие пастельные тона
- **Акценты**: Зеленый для подсказок (#22c55e)

### Компоненты
- **Игровое поле**: 3x3 сетка с анимациями
- **Модальные окна**: Результаты игры
- **Кнопки**: С градиентами и hover эффектами
- **Иконки**: Lucide React иконки

## 🔧 Технический стек

### Backend
- **Python 3.10+**
- **FastAPI** - веб-фреймворк
- **httpx** - HTTP клиент для Telegram API
- **python-dotenv** - управление переменными окружения

### Frontend
- **React 18** - UI библиотека
- **TypeScript** - типизированный JavaScript
- **Vite** - сборщик и dev сервер
- **Tailwind CSS** - утилитарный CSS фреймворк
- **Lucide React** - иконки

## 🛡️ Безопасность

### Рекомендации
- ❗ **Никогда не коммитьте .env файл** в git
- 🔐 **Используйте переменные окружения** в production
- 🔄 **Регулярно обновляйте токены** бота
- 🚫 **Не передавайте токены в публичных чатах**

### В production используйте
- Docker контейнеры
- Системы управления секретами (AWS Secrets Manager, HashiCorp Vault)
- CI/CD переменные окружения
- HTTPS для всех подключений

## 📊 Мониторинг

### Health Check
```bash
curl http://localhost:8000/api/health
```

### Debug информация
```bash
curl http://localhost:8000/api/debug
# Показывает настройки Telegram и состояние переменных
```

### Логи
Сервер выводит подробные логи:
- 📤 Отправка сообщений в Telegram
- 🎮 Обработка результатов игр
- ⚠️ Ошибки и предупреждения

## 🐛 Решение проблем

### Backend не запускается
```bash
# Проверьте зависимости
pip list | grep fastapi

# Проверьте порт
lsof -i :8000
```

### Telegram не работает
1. Проверьте токен и chat_id в .env
2. Убедитесь, что отправили сообщение боту
3. Проверьте API: `https://api.telegram.org/bot<TOKEN>/getMe`

### Frontend не подключается
1. Убедитесь, что backend запущен на порту 8000
2. Проверьте CORS настройки
3. Обновите VITE_API_URL в frontend/.env

## 🚀 Деплой

### Backend (Railway, Render, Heroku)
1. Подключите репозиторий
2. Установите переменные окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
3. Настройте автодеплой

### Frontend (Vercel, Netlify)
1. Подключите репозиторий
2. Настройте build команду: `npm run build`
3. Обновите `VITE_API_URL` на URL backend

## 📝 Лицензия

MIT License - используйте свободно для любых целей.

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел "Решение проблем"
2. Изучите логи сервера
3. Проверьте API endpoints

---

**Игра создана с ❤️ для демонстрации современных веб-технологий и интеграции с Telegram API.**
