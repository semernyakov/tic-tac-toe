# 🎮 Крестики-нолики — Полная инструкция по установке

## 📋 Содержание
1. [Создание Telegram бота](#1-создание-telegram-бота)
2. [Установка Backend](#2-установка-backend)
3. [Настройка Frontend](#3-настройка-frontend)
4. [Запуск приложения](#4-запуск-приложения)
5. [Деплой](#5-деплой)

---

## 1. Создание Telegram бота

### Шаг 1: Создайте бота через BotFather

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `Крестики-нолики Промокоды`)
4. Введите username бота (например: `tictactoe_promo_bot`)
5. BotFather выдаст вам **токен бота** — сохраните его!

```
Пример токена: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

### Шаг 2: Получите Chat ID

1. Отправьте любое сообщение вашему боту
2. Откройте в браузере:
```
https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
```
3. Найдите `"chat":{"id":123456789}` — это ваш Chat ID

---

## 2. Установка Backend

### Шаг 1: Установите Python 3.10+

```bash
python --version  # Проверьте версию
```

### Шаг 2: Создайте структуру проекта

```bash
mkdir tic-tac-toe-game
cd tic-tac-toe-game
mkdir backend
cd backend
```

### Шаг 3: Создайте виртуальное окружение

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### Шаг 4: Установите зависимости

Создайте файл `requirements.txt`:
```txt
fastapi==0.109.0
granian==1.2.0
httpx==0.26.0
pydantic==2.5.3
uvicorn==0.27.0
python-dotenv==1.0.0
```

Установите:
```bash
pip install -r requirements.txt
```

### Шаг 5: Создайте файлы backend

**main.py** (скопируйте код из артефакта выше)

**run_granian.py**:
```python
from granian import Granian
from granian.constants import Interfaces

def run():
    Granian(
        "main:app",
        address="0.0.0.0",
        port=8000,
        interface=Interfaces.ASGI,
        workers=1,
        reload=True
    ).serve()

if __name__ == "__main__":
    run()
```

**config.py**:
```python
import os
from dotenv import load_dotenv

load_dotenv()

TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN", "")
TELEGRAM_CHAT_ID = os.getenv("TELEGRAM_CHAT_ID", "")
```

**.env**:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

### Шаг 6: Обновите main.py для использования .env

Замените строки с токенами в `main.py`:
```python
from config import TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID
```

---

## 3. Настройка Frontend

### Вариант A: Использование артефакта Claude (Быстрый старт)

React-приложение уже работает в артефакте выше! Просто обновите переменные:

```javascript
const API_URL = 'http://localhost:8000';
const TELEGRAM_BOT_TOKEN = 'ваш_токен';
const TELEGRAM_CHAT_ID = 'ваш_chat_id';
```

### Вариант B: Полноценная установка с Node.js

```bash
cd ..  # Вернитесь в корневую папку проекта
npx create-react-app frontend
cd frontend
npm install lucide-react
```

Замените содержимое `src/App.js` кодом из React-артефакта выше.

---

## 4. Запуск приложения

### Запуск Backend

```bash
cd backend
# Активируйте виртуальное окружение, если не активировано
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Запуск с Granian (рекомендуется)
python run_granian.py

# Или с Uvicorn
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Backend запустится на `http://localhost:8000`

### Запуск Frontend

**Вариант A: Использование артефакта**
- Просто откройте артефакт в Claude.ai — он уже работает!

**Вариант B: Node.js приложение**
```bash
cd frontend
npm start
```

Frontend откроется на `http://localhost:3000`

---

## 5. Деплой

### Backend на Railway.app (Бесплатно)

1. Создайте аккаунт на [railway.app](https://railway.app)
2. Нажмите "New Project" → "Deploy from GitHub"
3. Добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
4. Railway автоматически определит FastAPI и запустит приложение

### Frontend на Vercel (Бесплатно)

1. Создайте аккаунт на [vercel.com](https://vercel.com)
2. Импортируйте ваш GitHub репозиторий
3. Vercel автоматически задеплоит React-приложение
4. Обновите `API_URL` на URL вашего Railway backend

### Альтернатива: Render.com

Оба сервиса можно задеплоить на [render.com](https://render.com) бесплатно.

---

## 🎯 Проверка работы

1. **Проверьте backend**:
```bash
curl http://localhost:8000/api/health
```

2. **Проверьте Telegram**:
   - Отправьте сообщение боту
   - Убедитесь, что бот получил сообщение

3. **Играйте в игру**:
   - Откройте frontend
   - Сыграйте партию
   - Проверьте, пришло ли сообщение в Telegram

---

## 🐛 Решение проблем

### Backend не запускается
```bash
# Проверьте установку зависимостей
pip list | grep fastapi

# Проверьте порт
lsof -i :8000  # Linux/Mac
netstat -ano | findstr :8000  # Windows
```

### Telegram не получает сообщения
1. Проверьте токен и chat_id в `.env`
2. Убедитесь, что отправили сообщение боту хотя бы раз
3. Проверьте URL: `https://api.telegram.org/bot<TOKEN>/getMe`

### CORS ошибки
Убедитесь, что в `main.py` настроен CORS middleware (уже включен в код).

---

## 📦 Структура проекта

```
tic-tac-toe-game/
├── backend/
│   ├── venv/
│   ├── main.py
│   ├── config.py
│   ├── run_granian.py
│   ├── requirements.txt
│   └── .env
└── frontend/
    ├── src/
    │   └── App.js
    ├── public/
    └── package.json
```

---

## 🚀 Готово!

Теперь у вас есть полностью рабочая игра Крестики-нолики с:
- ✅ Красивым дизайном для женской аудитории
- ✅ Умным AI противником
- ✅ Генерацией промокодов
- ✅ Интеграцией с Telegram
- ✅ Backend на FastAPI + Granian
- ✅ Frontend на React

**Рабочая ссылка**: После деплоя на Vercel/Railway вы получите постоянный URL!